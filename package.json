{
  "name": "crewup",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "web": "npm run dev -w @crewup/web",
    "mobile": "npm run start -w @crewup/mobile",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "pg": "^8.21.0",
    "turbo": "^2.9.16"
  }
}
